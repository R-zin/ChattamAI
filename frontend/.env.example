# Optional. Base URL of the FastAPI RAG backend (no trailing slash).
# If unset, the UI runs fully on built-in realistic mock data (recommended for
# the public Vercel demo, since the backend binds to localhost in dev).
# For local dev against the real API use: http://127.0.0.1:8000
VITE_API_URL=
